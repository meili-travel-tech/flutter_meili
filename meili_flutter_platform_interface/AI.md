# meili_flutter_platform_interface AI Notes

Purpose: Platform interface package for the federated plugin.

Notes
- Not currently wired into meili_flutter or the platform packages.
- MethodChannel name in the template is `meili_flutter`.
